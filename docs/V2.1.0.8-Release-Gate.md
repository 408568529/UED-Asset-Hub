# V2.1.0.8 Release Gate

状态：PASS

## 范围

本次收口包含受限 Agent Preset、Knowledge Gateway MCP、DeepSeek 模型设置与 Credential 对接，以及只读读取链路和 E2E Fixture 校验。

Credential 始终由 DSH Runtime 的受限存储管理；不进入 Asset Hub `DATA_DIR`、Git、浏览器持久化存储、普通日志或本记录。

## Runtime 与 Source

- DSH：`0.1.1-rc.2`
- 官方 Git Source commit：`b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`
- 官方 DSH 工作树：clean
- DSH 官方源码：未修改

## 最终验收

- DeepSeek Runtime Connectivity：PASS。真实 Restricted Session 已完成模型调用。
- MCP Discovery：PASS。仅发现 `get_knowledge_asset`、`search_knowledge`、`search_micro_specs`。
- Case A–E：PASS。真实 Agent 通过 MCP 检索 Micro Spec、Prompt、Skill、精确规则、无结果场景与 citation。
- Direct FS Boundary：PASS。Restricted Session 不含 Shell、任意文件系统、文件搜索、Web、Editor 或 Terminal Tool；直接 Fixture 路径读取请求未产生 Tool Call。
- Restricted Preset：PASS。Agent Proxy 强制新 Session 使用 `asset-hub-restricted`。
- DATA_DIR 零业务副作用：PASS。受保护业务文件集合、文件数量与 hash 在最终 E2E 前后保持一致；Prompt `viewCount` / `copyCount` 与 Skill `downloadCount` 未变化。

## Fixture 与审计判据

业务数据与应用审计分开校验：

- 受保护集合包含所有业务文件，排除唯一审计文件 `meta/logs.json`。
- 校验受保护文件清单、数量与确定性 hash，并拒绝任何新增、删除或修改。
- 审计文件只允许显式 Allowlist 的合法登录记录；基线审计记录不得删除或修改。
- 缺失业务 JSON 的普通读取返回空数据，不创建 fallback 文件。只有明确的写操作可初始化或更新业务文件。

## 自动验证

- `npm run build`
- `npm run typecheck`
- `npm run verify:runtime-data`
- `npm run verify:knowledge-readonly`
- `npm run verify:knowledge-write`
- `npm run verify:knowledge-gateway`
- `npm run verify:agent-runtime`
- `npm run verify:agent-boundary`
- `npm run verify:e2e-fixture -- verify ...`

未执行 push、merge 或 tag。
